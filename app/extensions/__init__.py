"""Extensions package.

Holds initialization logic and shared extension instances used by the app
(`SQLAlchemy`, `JWTManager`, `Flask-Migrate`, `CORS`). See
`app.extensions.extensions` for concrete setup.
"""
